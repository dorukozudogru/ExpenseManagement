@model IEnumerable<ExpenseManagement.Models.EnergyMonthlies>

@{
    ViewData["Title"] = "Aylık Üretim";
}

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-primary">
                    <div class="card-header">
                        <h5 class="box-title">
                            <i class="fa fa-calendar-alt"></i>&nbsp;Aylık Üretim
                            <a class="btn btn-light text-primary btn-sm float-right" asp-action="MonthlyCreate"><i class="fa fa-plus"></i>&nbsp;&nbsp;Yeni Oluştur</a>
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <div class="card-body">
                                <table id="ItemsTable" class="table table-bordered table-striped" width="100%">
                                    <thead>
                                        <tr>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Id)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Year)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Month)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.ProducedKw)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.ConsumedKw)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.DistributionFee)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Amount)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.TAX)
                                            </th>
                                            <th>İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tfoot>
                                        <tr>
                                            <td style="font-weight:bold" colspan="3">Yıllık Toplam:</td>
                                            <td style="font-weight:bold" id="totalProducedKw"></td>
                                            <td style="font-weight:bold" id="totalConsumedKw"></td>
                                            <td style="font-weight:bold" id="distributionFee"></td>
                                            <td style="font-weight:bold" id="totalAmount"></td>
                                            <td style="font-weight:bold" id="totalTAX"></td>
                                            <td style="font-weight:bold"></td>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts{
    <script>
        (function () {
            var generateCustomerTable = $("#ItemsTable").DataTable({
                "language": {
                    processing: '<i class="fas fa-sync fa-spin fa-3x fa-fw"></i><span class="sr-only">Yükleniyor...</span> ',
                    zeroRecords: "Hiçbir kayıt bulunamadı.",
                    emptyTable: "Kayıt bulunmamaktadır.",
                    search: "Ara:",
                    oPaginate: {
                        "sFirst": "İlk",
                        "sLast": "Son",
                        "sNext": "»",
                        "sPrevious": "«"
                    },
                    sLengthMenu: "Listelenen maksimum kayıt sayısı: _MENU_",
                    sInfo: "Toplam _TOTAL_ kayıttan _START_ - _END_ arası gösteriliyor",
                },
                "processing": true,
                "serverSide": true,
                "searchDelay": 1200,
                "stateSave": true,
                "stateDuration": -1,
                "searching": false,
                "order": [[ 0, "desc" ]],
                "ajax": {
                    "url": "/Energy/MonthlyPost",
                    "method": "POST"
                },
                "columns": [
                    { "data": "id" },
                    { "data": "year" },
                    { "data": "monthName" },
                    {
                        "data": "producedKw",
                        "render": function (data, type, full, meta) {
                            if (data != null) {
                                return currency(full.producedKw, { decimal: ',', separator: '.', symbol: '' }).format() + " kW";
                            }
                            return "-";
                        }
                    },
                    {
                        "data": "consumedKw",
                        "render": function (data, type, full, meta) {
                            if (data != null) {
                                return currency(full.consumedKw, { decimal: ',', separator: '.', symbol: '' }).format() + " kW";
                            }
                            return "-";
                        }
                    },
                    {
                        "data": "distributionFee",
                        "render": function (data, type, full, meta) {
                            if (data != null) {
                                return currency(full.distributionFee, { decimal: ',', separator: '.', symbol: '' }).format() + " ₺";
                            }
                            return "-";
                        }
                    },
                    {
                        "data": "amount",
                        "render": function (data, type, full, meta) {
                            if (data != null) {
                                return currency(full.amount, { decimal: ',', separator: '.', symbol: '' }).format() + " ₺";
                            }
                            return "-";
                        }
                    },
                    {
                        "data": "tax",
                        "render": function (data, type, full, meta) {
                            if (data != null) {
                                return currency(full.tax, { decimal: ',', separator: '.', symbol: '' }).format() + " ₺";
                            }
                            return "-";
                        }
                    },
                    {
                        "data": "actionId", render: function (data, type, row) {
                            return '<div class="btn-group"><button type="button" class="btn" data-toggle="dropdown"><span class="fa fa-angle-down"></span></button></button><ul class="dropdown-menu dropdown-menu-right" style="border: 1px solid #00c0ef"> <a class="dropdown-item" href="MonthlyEdit/' + row.id + '">Güncelle</a> <a class="dropdown-item" href="MonthlyDelete/' + row.id + '">Sil</a></ul></div>'
                        },
                        "orderable": false
                    }
                ],
                "ordering": true,
                "paging": true,
                "pagingType": "simple_numbers",
                "pageLength": 100
            });

            $.ajax({
                "url": "/Energy/YearlyPost",
                "method": "POST"
            }).done(function (response) {
                $('#totalProducedKw').html(currency(response[0], { decimal: ',', separator: '.', symbol: '' }).format() + " kW");
                $('#totalConsumedKw').html(currency(response[1], { decimal: ',', separator: '.', symbol: '' }).format() + " kW");
                $('#distributionFee').html(currency(response[2], { decimal: ',', separator: '.', symbol: '' }).format() + " ₺");
                $('#totalAmount').html(currency(response[3], { decimal: ',', separator: '.', symbol: '' }).format() + " ₺");
                $('#totalTAX').html(currency(response[4], { decimal: ',', separator: '.', symbol: '' }).format() + " ₺");
            });
        })(jQuery);
    </script>
}